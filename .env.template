# 复制为 .env 并按需修改：cp .env.template .env
# 未设置即为默认值。
# 更多可选项（缓存、指标、钱包登录、请求限流等）见源码 common/config 与现有注释。

# 服务监听端口
PORT=3011

# 会话加密密钥，必须自定义
SESSION_SECRET=replace_with_random_string

# 使用 MySQL/PostgreSQL 时的连接串（留空则使用 SQLite）
# SQL_DSN=user:pass@tcp(db:3306)/router

# 指定 SQLite 文件路径（默认 /data/one-api.db）
# SQLITE_PATH=/data/router.db

# 启用 Redis 缓存/会话
# REDIS_CONN_STRING=redis://:pass@redis:6379

# 节点角色 master / slave
# NODE_TYPE=slave

# 从数据库/Redis 同步配置间隔（秒）
# SYNC_FREQUENCY=60

# 从节点前端跳转地址
# FRONTEND_BASE_URL=https://openai.example.com

# 上游请求代理
# RELAY_PROXY=http://127.0.0.1:7890

# 上游请求超时（秒）
# RELAY_TIMEOUT=120

# 钱包登录 JWT 密钥
WALLET_JWT_SECRET=dev_secret

# 钱包自动注册
AUTO_REGISTER_ENABLED=true

# 刷新 Cookie SameSite
# WALLET_REFRESH_COOKIE_SAMESITE=lax

# 刷新 Cookie Secure
# WALLET_REFRESH_COOKIE_SECURE=true

# CORS 允许来源（逗号分隔），留空则允许所有来源（会回显 Origin）
# 支持通配子域名，例如：https://*.example.com 或 *.example.com:3000
# CORS_ALLOWED_ORIGINS=http://localhost:5181,http://localhost:3011

# UCAN 授权（可选）
# UCAN_AUD=did:web:localhost:3011
# UCAN_RESOURCE=profile
# UCAN_ACTION=read

# 首启自动生成的 root 用户令牌
# INITIAL_ROOT_TOKEN=sk-xxx

# 首启自动生成的系统管理令牌
# INITIAL_ROOT_ACCESS_TOKEN=sk-xxx

# 时区
TZ=Asia/Shanghai

# 调试
DEBUG=false
# GIN_MODE=debug

# HTTP/HTTPS 代理（如需）
HTTPS_PROXY=http://localhost:7890
