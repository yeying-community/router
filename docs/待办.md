# 待办

本文档只保留当前仍未完成、且已经明确要做的事项。
已落地的能力不再重复记录，避免文档和代码长期偏离。

## 汇率治理

### 待补充事项

1. 汇率源容灾与降级

- 当前仅接入 `frankfurter` 单一来源。
- 需要补第二来源与失败降级策略（主/备切换、失败重试、状态告警）。

2. 汇率历史快照

- 当前只保留最新兑换参数。
- 需要补“按日快照”或“按同步批次快照”，用于审计、回放和问题追踪。

3. 自动同步可观测性增强

- 当前仅有最近成功/失败时间与错误文案。
- 需要补：
  - 连续失败计数
  - 同步耗时
  - 最近 N 次执行记录
  - 失败告警阈值

4. 汇率来源策略扩展

- 当前汇率来源是全局单一 provider。
- 需要评估是否支持按币种或按租户指定来源与更新策略。

## Tokenizer 方案

### 待补充事项

1. 评估 tokenizer 依赖替换

- 当前项目使用 `github.com/pkoukk/tiktoken-go`，模型映射表较旧。
- 评估是否替换为更底层、只负责 encoding 的库，例如 `github.com/tiktoken-go/tokenizer`。
- 不再依赖第三方库内置的“模型名 -> encoding”映射完整性。

2. 自维护模型到 encoding 的映射

- 在 Router 内部维护一份清晰的 `model/prefix -> encoding` 规则表。
- 至少覆盖：
  - `gpt-5* -> o200k_base`
  - `o1* / o3* / o4* -> o200k_base`
  - `gpt-realtime* / gpt-audio* / gpt-image* -> o200k_base`
  - 老的 `gpt-4* / gpt-3.5*` 继续映射到现有 encoding

3. 评估是否引入官方 tokenizer 能力

- OpenAI 官方维护的是 Python 的 `tiktoken`，不是 Go 库。
- 评估是否需要通过 sidecar / 服务化方式接入官方 tokenizer，实现更准确的 token 估算。

4. 降级策略与日志治理

- 对未知模型不要继续回退到 `gpt-3.5-turbo`。
- 优先回退到同家族模型对应的 encoding。
- 对可预期的新模型家族，不打印错误级别日志，避免污染日志面板。

## 充值链路治理

### 当前状态

- 充值中心“前往充值”已经改为先调用 `POST /api/v1/public/user/topup/orders`。
- 由 Router 后端生成内部订单 ID、`transaction_id` 和最终 `redirect_url`。
- 前端不再自行拼接 `username`、`user_id`、`transaction_id`。
- 真正的入账动作仍然发生在用户拿到兑换码后，调用 `POST /api/v1/public/user/topup`。
- `POST /api/v1/public/user/topup` 的接口兼容演进方案，见 [充值接口兼容演进方案](./充值接口兼容演进.md)。

### 待补充事项

1. 统一充值产品语义

- 明确区分“跳转外部充值页”和“Router 内部发放兑换码”两类模式。
- 避免页面文案、接口语义和实际业务行为不一致。

2. 兑换码发放链路

- 如果需要“立即获取兑换码”，应明确由 Router 后台生成兑换码，还是由外部充值系统回传兑换码。
- 补齐充值成功后的兑换码发放、状态变更、幂等校验和失败补偿。

3. 充值订单状态流转

- 当前已经有独立的充值订单记录和兑换入账记录展示。
- 当前也已补基础回调接口、状态推进和订单-兑换码关联。
- 后续仍需补不同支付方的回调协议规范、失败补偿，以及更完整的发码链路。

## 路由策略优化

### 待补充事项

1. 区分“请求内切换”和“后续请求避障”的运维配置

- 当前这两套机制容易被混淆。
- 需要在管理台和文档中分别表达：
  - 请求内切换：运行设置中的 `RetryTimes`（`0` 关闭，`>0` 尝试完剩余候选）
  - 后续请求避障：自动禁用、余额刷新、人工摘除

2. 渠道错误分类与熔断策略

- 当前上游限流、余额不足、鉴权失败、供应商网关异常，都可能呈现出相近的失败表象。
- 目标分类矩阵与策略基线见 [上游错误处理策略](./上游错误处理策略.md)。
- 后续实现重点仍是：窗口熔断、自动恢复、可观测性细化。

3. 端点能力可视化

- 当前 `channel_model_endpoints` 已参与真实选路与请求内重试，但后台还看不到端点能力状态。
- 需要在渠道详情或相关管理页展示：
  - 某个渠道下某个模型支持哪些端点
  - 哪些端点已被自动摘除
  - 摘除时间、来源原因、恢复状态

4. 端点能力恢复机制

- 当前 `unsupported_channel_endpoint` 已支持按 `channel + model + endpoint` 维度摘除。
- 后续需要补恢复路径，避免一旦摘除后长期无法自动回收：
  - 人工恢复
  - 模型同步时恢复
  - 定时探测恢复

5. 短时故障窗口熔断

- 当前对 `429`、`5xx`、超时，主要还是单次请求内切换。
- 后续需要增加窗口级熔断与自动恢复，避免短时间内持续命中明显不稳定的渠道：
  - 连续失败阈值
  - 熔断持续时间
  - 半开探测
  - 恢复后的重新放量

6. JWT/UCAN 令牌记账策略显式化

- 当前使用钱包 JWT / UCAN 调用 `/v1/*` 时，会自动选取该用户“最早创建且可用”的 token 作为记账对象。
- 这会导致“请求实际记账 token”与“用户当前查看 token”可能不一致，造成令牌已用统计理解偏差。
- 需要补：
  - 管理台与文档明确展示该默认策略
  - 支持按用户配置默认记账 token，或提供“必须显式 sk 调用”开关
  - 请求日志和用户侧令牌页增加“本次请求命中的 token”可观测信息
